---
title: "Playbooks"
description: "Playbook listing and apply workflow"
---

Use `playbooks` to list/get templates and apply them to provisioning flows.

Supported actions: `all`, `list`, `get`, `categories`, `apply`.
